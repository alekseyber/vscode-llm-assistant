# План реализации бэклога — 19:30

**Дата:** 05.08.2026
**После:** исправления summary (19:00)

---

## Бэклог

### B1. Связка История ↔ Сессии
**Суть:** клик по записи в 📊 Истории → переключение чата на ту сессию

**Проблема:** сейчас история и сессии независимы
**Решение:**
1. Сохранять `sessionId` в `RunEntry` при записи
2. В HistoryViewProvider добавить обработчик `switchToSession`
3. В `handleMessage` → postMessage в ChatViewProvider для переключения
4. ChatViewProvider: `switchSession(sessionId)` — загружает сессию в чат

**AC:** клик на запись → чат показывает ту же сессию, селектор сессий обновляется

---

### B2. Индикация проблем деградации функциональности
**Суть:** пользователь должен видеть КАКИЕ ограничения активны

**Проблема:** сейчас allow-list невидим (агент показывает все инструменты в промпте)
**Решение:**
1. В system prompt добавлять информацию об активных ограничениях
2. В WebView — плашка «⚠️ Allow-list активен: read_file, search_files»
3. Аналогично для MCP: «🔌 MCP: filesystem (14 tools)»

**AC:** пользователь видит активные ограничения в WebView и system prompt

---

### B3. MCP Agent интеграционный тест
**Суть:** третий тест из набора (был обещан но не сделан)

**Решение:**
1. Мок McpClient с фиктивными инструментами
2. Проверка что инструменты добавляются в tools массив
3. Проверка что allow-list фильтрует MCP-инструменты

**AC:** тест проходит в `npm run test:mocked`

---

### B4. Hermes gateway ↔ function calling
**Суть:** сейчас hermes не поддерживает агента — нужно либо починить, либо документировать

**Вариант А:** добавить поддержку tools в Hermes gateway (если API DeepSeek позволяет)
**Вариант Б:** оставить как есть — пользователь переключает провайдера для агента

**AC:** решение принято и задокументировано в README

---

### B5. Очистка незакоммиченных изменений
**Суть:** после ручного тестирования много файлов с незакоммиченными правками

**Решение:**
1. `git status` → проверить все изменённые файлы
2. `git diff` по каждому → оценить что нужно коммитить
3. Коммит + пуш

**AC:** `git status` чистый

---

## Приоритет

| ID | Что | Приоритет | Время |
|----|-----|-----------|-------|
| B5 | Очистка кода | 🔴 Срочно | ~5 мин |
| B2 | Индикация ограничений | 🟡 Важно | ~20 мин |
| B1 | История↔Сессии | 🟢 Фича | ~30 мин |
| B3 | MCP тест | 🟢 Тест | ~15 мин |
| B4 | Hermes + агент | 🔵 Решение | ~5 мин |

## Порядок на 19:30

1. B5: очистка кода, коммит незакоммиченного
2. B2: индикация allow-list/MCP в WebView
3. B3: MCP интеграционный тест
4. B1: история → клик → сессия
5. B4: решение по Hermes + агент → README
6. Коммит + пуш
7. CI проверка
