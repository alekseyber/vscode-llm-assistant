# Release Notes — v0.5.3

## Основные изменения с v0.1.0

### 🤖 Агентный режим
- ReAct-цикл с function calling (до 5 итераций)
- 6 инструментов: `list_files`, `search_files`, `read_file`, `write_file`, `replace_in_file`, `run_terminal`
- Подтверждение операций записи (git-diff диалог)
- Настройка `llmAssistant.agent.requireConfirmation`

### 👁️ Vision
- Анализ изображений через Qwen/Qwen3-VL-32B-Instruct (SiliconFlow)
- Настройка `supportsVision: true` в провайдере
- Превью изображений в чате, поддержка 📎

### 💬 Чат
- Системный промпт (настраиваемый через `chat.systemPrompt` и `chat.agentSystemPrompt`)
- Кастомный промпт на каждый провайдер (`systemPrompt` в конфиге провайдера)
- Авто-контекст открытого файла
- Подстановка `${ENV_VAR}` в `apiKey` и `baseUrl`

### 📋 Сессии
- Авто-именование из первого сообщения
- Переключение, удаление, переименование (двойной клик)
- Сохранение между сессиями VS Code
- Кнопка «Поделиться с Hermes» (💬) — копирует сессию в буфер

### 🎨 UI
- Быстрые действия: 🔧 Исправить, 💡 Объяснить, ⚡ Оптимизировать
- Контекст-бар в шапке: `[████░░] 1071/4096`
- Стоимость токенов после ответа: `📊 156+42 токенов ≈ $0.0001`
- Сворачиваемые блоки кода ▼/▶
- Кнопка копирования для кода (📋)
- Git-diff диалог подтверждения
- Стилизованные тултипы при наведении
- Экспорт сессии в .md (📥)

### 🔧 Инфраструктура
- GitHub Actions CI — авто-прогон линтера и тестов при пуше
- 11 тестов SessionManager
- 9 тестов ProviderManager (включая `${VAR}` подстановку)
- try-catch защита от зависаний при активации

### 🐛 Исправлено
- Ошибка загрузки чата (marked.min.js не в VSIX)
- Дублирование сообщений пользователя
- Стриминг без разрывов (textContent вместо innerHTML)
- 401 при неправильном SiliconFlow base_url
- Контекст-бар удалялся при очистке истории
- Сессии не очищались при переключении (restoreHistory)
- Переменные окружения в настройках провайдеров

---

## Changelog

### v0.5.3
- fix: restoreHistory всегда очищает контейнер (сессии)

### v0.5.2
- ci: GitHub Actions — авто-прогон тестов

### v0.5.1
- test: SessionManager + ${VAR} env подстановка

### v0.5.0
- feat: UI overhaul — цветные плашки, контекст-бар в шапке, быстрые действия, сворачивание кода

### v0.4.5
- fix: токены обновляются в агентном режиме

### v0.4.4
- feat: run_terminal в агентном режиме

### v0.4.3
- fix: context-bar вне messagesContainer

### v0.4.0
- fix: иконки 💬/📥, контекст-бар всегда виден

### v0.3.9
- feat: индикатор заполнения контекстного окна

### v0.3.8
- feat: стоимость токенов + экспорт сессии в .md

### v0.3.7
- feat: drag & drop (убран в v0.4.3 — не работает в WebView)

### v0.3.6
- feat: превью изображений в чате

### v0.3.4
- fix: 401 в runAgentLoop — использовать provider.createWithTools

### v0.3.3
- fix: изображение и текст в одном сообщении

### v0.3.1
- feat: поддержка vision-моделей

### v0.3.0
- ui: стилизованные тултипы

### v0.2.9
- ui: двухрядная шапка

### v0.2.8
- feat: кнопка «Поделиться с Hermes»

### v0.2.0
- feat: подтверждение операций записи (git-diff диалог)

### v0.1.0
- Начальный релиз: 4 режима, мульти-провайдер
