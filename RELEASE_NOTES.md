# Release Notes — v0.12.0

## 🗂 Сессии: полный жизненный цикл

- Сессии хранятся в append-only логе — история переживает перезапуск VS Code
- 📋 «Копировать сессию в буфер» и 📥 «Экспорт в Markdown» — полная транскрипция с tool-шагами (что агент читал, писал, запускал)
- Команда «Экспортировать сессию» — сохранение `.md` через системный диалог
- «Скопировать сессию (fork)» и «Удалить все сессии»
- Длинные диалоги автоматически сжимаются: «Краткое содержание» + обрезка старых сообщений

## 🔧 Исправления

- Отмена ⏹️ в оркестраторе больше не падает с ошибкой «Request was aborted»
- Tool-шаги оркестратора видны в экспорте (раньше — только итоговые ответы воркеров)
- Воркер без финального ответа помечается ⚠️ и «Лимит» в истории (не «Успех»)
- Индикатор токенов показывает реальный лимит из настроек, а не 4096

## ✅ Тесты

- **401 юнит-тест** + E2E в реальном VS Code
- CI: spec-validate → lint → unit → e2e

---

## 📖 Документация (v0.11.3)

- README консолидирован в единый пользовательский док: «Установка», «Устранение проблем», структура и примеры `.llma/`, цены моделей `{name, pricing}`
- Удалены устаревшие дубли `instructions/CONFIG.md` (v0.7.0) и `docs/USER-GUIDE.md` (v0.1.0); внутренние материалы (бэклог, планы, QA, деплой) вынесены из публичного репо
- Исправлены устаревшие цифры: 26 → 36 spec-файлов

---

## 🎨 Иконка (v0.11.2)

- Добавлена иконка расширения: жирная low-poly «a» в янтарном градиенте (`media/icon.png`)

---

## 📖 Документация (v0.11.1)

- README актуализирован: убрана версия из заголовка, обновлены фичи (AI-ревью, слэш-команды, 9 инструментов агента) и настройки

---

## 🔍 Standalone AI-ревью (v0.11.0)

- Команда «LLM Assistant: Review File» — ревью активного файла или выделения
- Отдельный ReviewerAgent (ReAct с `read_file`/`search_files`/`list_files`) — реально читает код и связанные файлы
- Отчёт: стиль / безопасность / корректность / оптимизация, замечания по приоритету (🔴 критично / 🟠 важно / 🟡 мелко)

## 🖥 Компактная сводка + широкое окно

- В сайдбаре — строка-сводка: «🔍 файл — стоимость ↗»
- Клик → широкое окно с полным markdown-отчётом (как окно подтверждения изменений)

## 📋 Свои правила ревью

- `.llma/agents/reviewer.md` — задай правила ревью под проект («игнорируй TODO», «только безопасность», «наш стиль: 4 пробела»)
- Подхватываются автоматически и дописываются к базовому промпту ревьюера

## ✅ Тесты

- **335 юнит-тестов** + **20 E2E-тестов** (реальный VS Code)
- CI: spec-validate → lint → unit → e2e
