# План: MA-6 + MA-7 — Multi-Agent Harness (финал)

## MA-6: Agent-to-Agent делегирование

**Суть:** Воркер через tool call может запросить помощь другого воркера. Оркестратор создаёт под-воркера, результат возвращается первому.

### AC

| ID | Критерий |
|----|----------|
| MA-6.1 | Воркер может вызвать `delegate_to_agent(role, task)` как tool call |
| MA-6.2 | Оркестратор создаёт под-воркера, выполняет задачу, возвращает результат |
| MA-6.3 | Под-воркер изолирован: свой контекст, не видит историю родителя |

### Этапы

| Этап | Описание | AC |
|-------|----------|----|
| 6.1 | Инструмент `delegate_to_agent` в ChatAgentTools | MA-6.1 |
| 6.2 | Оркестратор обрабатывает делегирование | MA-6.2 |
| 6.3 | Изоляция контекста под-воркера | MA-6.3 |

## MA-7: Cost tracking per agent

**Суть:** Счётчик токенов/денег на каждого воркера в оркестраторе. Итоговый отчёт после оркестрации.

### AC

| ID | Критерий |
|----|----------|
| MA-7.1 | WorkerResult содержит `cost` (USD) |
| MA-7.2 | MultiAgentResult содержит `totalCost` и `costPerWorker` |
| MA-7.3 | После оркестрации — сводка в чат: токены + стоимость по воркерам |

### Этапы

| Этап | Описание | AC |
|-------|----------|----|
| 7.1 | `WorkerResult.cost` — расчёт через `calculateCost()` | MA-7.1 |
| 7.2 | `MultiAgentResult.totalCost` + `costPerWorker` | MA-7.2 |
| 7.3 | Вывод сводки в `handleOrchestrate` | MA-7.3 |

## Гейты

| Gate | Блокирует | Критерий |
|------|-----------|----------|
| G1 | 6.2 | `npm run compile` + `test:mocked` (0 failures) |
| G2 | 7.1 | G1 + тесты AgentWorker |
| G3 | Релиз | Все тесты, lint, spec-validate |

## Оценка

- **MA-6:** 3-4 часа
- **MA-7:** 2-3 часа
- **Всего:** 5-7 часов
