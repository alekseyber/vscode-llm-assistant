# PLAN: Очистка инжекта ⚠️ после ask_user (MA-1.11)

## Цель

После выполнения инструмента `ask_user` удалять инжектированное system-сообщение (⚠️ на позиции 1) из цепочки сообщений ReAct-цикла, чтобы:
- Не засорять контекст устаревшей директивой
- Не сбивать модель на последующих шагах цикла

## Этапы

### Stage 1: Реализация в AgentWorker

| AC | Критерий | Статус |
|----|----------|--------|
| MA-1.11 | После выполнения `ask_user`: если `messages[1]` — system с `⚠️`, удалить через `splice(1, 1)` | planned |

**Действия:**
1. В `AgentWorker.run()`, после успешного `tool.execute(args)` для `ask_user` — добавить проверку и удаление инжекта
2. `npm run compile` — чистая компиляция
3. `npx tsc -p tsconfig.test.json` — компиляция тестов
4. `npm run test:mocked` — существующие тесты не должны сломаться
5. `npm run lint` — 0 ошибок
6. Обновить AC: MA-1.11 → ✅
7. Коммит + push

**Gate 1:** Все тесты зелёные → переход к Stage 2.

### Stage 2: Приёмка

| AC | Критерий | Статус |
|----|----------|--------|
| - | CHANGELOG обновлён | planned |
| - | PR готов к мёрджу | planned |

## Затронутые файлы

| Файл | Изменение |
|------|-----------|
| `src/modes/apply/AgentWorker.ts` | +5 строк: проверка и удаление инжекта после ask_user |
| `specs/AgentWorker.md` | ✅ обновлён (контракт + AC MA-1.11 + детали + история) |
| `specs/ChatViewProvider.md` | ✅ обновлён (детали реализации) |
| `CHANGELOG.md` | +1 строка |
